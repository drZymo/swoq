<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Swoq.InfraUI.Views"
             xmlns:vm="clr-namespace:Swoq.InfraUI.ViewModels"
             x:Class="Swoq.InfraUI.Views.GameObservationView"
             x:DataType="vm:GameObservationViewModel"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             Background="Black"
             Foreground="White"
             Name="self">
    <Grid ColumnDefinitions="*,auto">

        <Grid Grid.Column="0" Background="Black" Grid.RowDefinitions="auto,*,auto">
            <TextBlock Grid.Row="0" FontSize="24" HorizontalAlignment="Center" IsVisible="{Binding #self.ShowHeader}">
                <Run Text="{Binding UserName, Mode=OneWay}" />
                <Run> - Level </Run>
                <Run Text="{Binding Level, Mode=OneWay}" />
            </TextBlock>

            <Image Grid.Row="1" Source="{Binding Overview.Image}" Stretch="Uniform" RenderOptions.BitmapInterpolationMode="None"/>
        </Grid>

        <Grid Grid.Column="1" RowDefinitions="auto,auto,auto,*">
            <local:GroupBox Grid.Row="0" Header="Game">
                <StackPanel Orientation="Vertical">
                    <TextBlock>
                        <Run>Tick:</Run>
                        <Run Text="{Binding Tick, Mode=OneWay}" />
                    </TextBlock>
                    <TextBlock>
                        <Run>Result:</Run>
                        <Run Text="{Binding ActionResult, Mode=OneWay}" />
                    </TextBlock>
                </StackPanel>
            </local:GroupBox>

            <local:PlayerObservationView Grid.Row="1" DataContext="{Binding Player1, Mode=OneWay}" Title="Player"/>

            <Grid Grid.Row="2" IsVisible="{Binding HasPlayer2, Mode=OneWay}">
                <local:PlayerObservationView DataContext="{Binding Player2, Mode=OneWay}" Title="Player #2" />
            </Grid>
        </Grid>

        <Grid Grid.Column="0" ColumnDefinitions="*,auto,*" RowDefinitions="*,auto,auto,*" IsVisible="{Binding IsFinished}">
            <Panel Grid.Row="0" Grid.RowSpan="4" Grid.Column="0" Grid.ColumnSpan="3" Opacity="0.75" Background="Black" />
            <TextBlock Grid.Row="1" Grid.Column="1" FontSize="18" HorizontalAlignment="Center" Text="Game finished"/>
            <TextBlock Grid.Row="2" Grid.Column="1" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Text="{Binding FinishedResult}"/>
        </Grid>
    </Grid>
</UserControl>
